stages:
  - test

default:
  image: python:3.12-slim

variables:
  UV_CACHE_DIR: "$CI_PROJECT_DIR/.uv-cache"

cache:
  paths:
    - .uv-cache/

test-application:
  stage: test
  before_script:
    - pip install uv
    - uv sync --frozen
  script:
    - uv run python -m compileall -q src